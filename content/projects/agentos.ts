import type { Project } from "./schema";

// MISSING VERIFIED SOURCE: AgentOS repository + documentation.
// Sections intentionally absent until extracted from real material (Phase 5).
export const agentos: Project = {
  slug: "agentos",
  name: "AgentOS",
  index: "01",
  status: "pending",
  featured: true,
  sections: [],
};
